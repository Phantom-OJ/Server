<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sustech.edu.phantom.dboj.mapper.GroupMapper">
    <select id="getAllGroups" resultType="sustech.edu.phantom.dboj.entity.Group">
        select id, description
        from "group"
        where valid;
    </select>
    <select id="getGroups" resultType="sustech.edu.phantom.dboj.entity.Group">
        select id, description
        from "group"
        where valid
        and id in (
        <foreach collection="ids" index="index" item="id" separator="," close=");">
            #{id}
        </foreach>
    </select>
    <select id="getAssignmentGroup" resultType="sustech.edu.phantom.dboj.entity.Group">
        select id, description
        from "group"
        where valid
          and id in (
            select group_id
            from assignment_group
            where valid
              and assignment_id = #{aid});
    </select>
    <select id="getStudentGroup" resultType="sustech.edu.phantom.dboj.entity.Group">
        select id, description
        from "group"
        where valid
          and id in (select group_id from user_group where valid and user_id = #{sid});
    </select>
</mapper>